Use the website-analyzer agent to help with this request.

User request: $ARGUMENTS

If no arguments provided, greet the user and ask what they'd like to analyze. Show a few example requests:
- "scan example.com for bugs"
- "find deprecated jQuery on my-site.com"
- "create a project for example.com"
- "list my recent scans"
